apiVersion: v1
kind: Service
metadata:
  name: {{ include "applywise-ai.fullname" . }}
  labels:
    {{- include "applywise-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "applywise-ai.selectorLabels" . | nindent 4 }}
