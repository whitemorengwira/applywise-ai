apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "applywise-ai.fullname" . }}-config
  labels:
    {{- include "applywise-ai.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
