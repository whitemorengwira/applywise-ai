{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "collapsed": false,
      "gridPos": { "h": 1, "w": 24, "x": 0, "y": 0 },
      "id": 100,
      "title": "Application CRM & Career Operating System",
      "type": "row"
    },
    {
      "id": 1,
      "gridPos": { "h": 4, "w": 4, "x": 0, "y": 1 },
      "type": "stat",
      "title": "Jobs Analyzed",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "expr": "sum(applywise_jobs_analyzed_total) or vector(0)",
          "legendFormat": "Jobs",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "color": { "fixedColor": "blue", "mode": "fixed" }
        }
      }
    },
    {
      "id": 2,
      "gridPos": { "h": 4, "w": 5, "x": 4, "y": 1 },
      "type": "stat",
      "title": "Total Applications",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "expr": "sum(applywise_applications_created_total) or vector(0)",
          "legendFormat": "Applications",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "color": { "fixedColor": "purple", "mode": "fixed" }
        }
      }
    },
    {
      "id": 3,
      "gridPos": { "h": 4, "w": 5, "x": 9, "y": 1 },
      "type": "stat",
      "title": "Tailored CVs",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "expr": "sum(applywise_cv_tailor_generations_total) or vector(0)",
          "legendFormat": "CVs",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "color": { "fixedColor": "green", "mode": "fixed" }
        }
      }
    },
    {
      "id": 4,
      "gridPos": { "h": 4, "w": 5, "x": 14, "y": 1 },
      "type": "stat",
      "title": "Cover Letters",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "expr": "sum(applywise_cover_letter_generations_total) or vector(0)",
          "legendFormat": "Letters",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "color": { "fixedColor": "orange", "mode": "fixed" }
        }
      }
    },
    {
      "id": 5,
      "gridPos": { "h": 4, "w": 5, "x": 19, "y": 1 },
      "type": "stat",
      "title": "Interview Sessions",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "expr": "sum(applywise_interview_sessions_total) or vector(0)",
          "legendFormat": "Sessions",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "color": { "fixedColor": "teal", "mode": "fixed" }
        }
      }
    },
    {
      "id": 6,
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 5 },
      "type": "bargauge",
      "title": "Application Pipeline by Stage",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "expr": "sum(applywise_applications_created_total) by (stage)",
          "legendFormat": "{{stage}}",
          "refId": "A"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "color": { "mode": "palette-classic" }
        }
      },
      "options": {
        "orientation": "horizontal",
        "displayMode": "gradient",
        "showUnfilled": true
      }
    },
    {
      "id": 7,
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 5 },
      "type": "timeseries",
      "title": "Document Generation Velocity Over Time",
      "datasource": { "type": "prometheus", "uid": "${DS_PROMETHEUS}" },
      "targets": [
        {
          "expr": "sum(increase(applywise_cv_tailor_generations_total[1h]))",
          "legendFormat": "CV Tailoring",
          "refId": "A"
        },
        {
          "expr": "sum(increase(applywise_cover_letter_generations_total[1h]))",
          "legendFormat": "Cover Letters",
          "refId": "B"
        }
      ],
      "fieldConfig": {
        "defaults": {
          "unit": "short",
          "custom": {
            "drawStyle": "bars",
            "fillOpacity": 40
          }
        }
      }
    }
  ],
  "refresh": "30s",
  "schemaVersion": 38,
  "tags": ["applywise", "business-intelligence", "crm"],
  "time": { "from": "now-6h", "to": "now" },
  "timezone": "browser",
  "title": "ApplyWise AI — Business Intelligence & Pipeline",
  "uid": "applywise-business-intelligence",
  "version": 1
}
